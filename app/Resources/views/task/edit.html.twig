{% extends 'base.html.twig' %}

{% block content %}
    <h1>Task edit</h1>

    {{ form_start(edit_form, {'action': path('task_edit', {'id': task.id}) }) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="indented"/>
    {{ form_end(edit_form) }}

    <p>
        <a href="{{ path('task_index') }}">Back to the list</a>
    </p>
{% endblock %}
