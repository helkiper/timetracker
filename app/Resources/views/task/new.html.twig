{% extends 'base.html.twig' %}

{% block content %}
    <h1>Task creation</h1>

    {{ form_start(form, {'action': path('task_new')}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="indented"/>
    {{ form_end(form) }}

    <p>
        <a href="{{ path('task_index') }}">Back to the list</a>
    </p>
{% endblock %}
